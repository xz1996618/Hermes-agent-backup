# Hermes Agent 项目配置文件
# 工作目录：E:\13_WorkSpaceForHermes

## 知识库
位于 `01_bphe_knowledge_base/` 下，包含 62 篇板式换热器相关文献（PDF/Word/PPT）。
回答板换专业问题时，优先搜索此目录中的内容。

## 工作目录
默认工作目录为 E:\13_WorkSpaceForHermes，所有文件操作默认在此目录下。

## 规则
1. 回答板换相关问题：先查知识库 → 找不到再搜外部资源
2. 引用知识库内容时：必须注明具体文献名
3. 创建 Skill 前必须先提提案，用户同意后才能创建
4. 所有 profile 共用此知识库
